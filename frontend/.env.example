# Leave blank when using the Vite development proxy. For a deployed frontend,
# include the API path, for example: https://api.example.com/api
VITE_API_BASE_URL=
# Legacy alias supported for existing local setups.
VITE_API_URL=
